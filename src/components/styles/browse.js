import styled from "styled-components";

export const Container = styled.div`
    width:100%;
    background: url(${({bgImage})=>bgImage});
    height: 100%;
`;

export const BrowseTheme = styled.div`

`;

export const ShowCase = styled.div`

`;